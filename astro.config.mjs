import { defineConfig } from 'astro/config';

// Bright Picks — Kitchen Gadgets. Static site, deployed to Cloudflare Pages.
export default defineConfig({
  site: 'https://kitchen-gadgets.pages.dev',
});
